# 中間発表後：システム・リファクタリング計画（第2形態）

## 1. 新たに判明した前提条件（Fact）
いすゞロジスティクスからのヒアリングにより、以下の現場のリアルな制約が確定しました。

*   **物流の波（季節変動）はない**：一定の時期に一定量の荷物が届く。
*   **【KPI】充填率80%の壁**：コンテナの積載率（体積・重量）が80%未満の場合は赤字となる。
*   **スケジュールの可視性**：月末ごとに、2週間〜1ヶ月先までの発注（荷物）リストが通達される。
*   **木箱の寿命と「前倒し」の限界**：木箱の耐久性の問題（防錆等）があり、倉庫内での保管・前倒し待機は**「最大3週間」**が限界。
*   **船での輸送期間**：1〜2ヶ月かかるため、現地の工場への「納期」は絶対である。
*   **現場での自由度**：梱包済みの荷物は現場で判別可能であり、コンテナ内で効率よく入れ替え（パズルのやり直し）が可能。

---

## 2. コンセプトの大転換（Paradigm Shift）

これまでは「静的な3Dパズル（空間のみの最適化）」を解いていましたが、ここからは**「時間軸（Time Axis）」を加えた4D最適化**へと移行します。

*   **旧目標**：「季節ごとの波を平準化し、年間コンテナ数を減らす」
*   **新目標**：「納期（SLA）を守りつつ、数週間先の未来の荷物を**前倒し（Pull）**したり、現在の荷物を次回に**繰り越し（Push）**することでデッドスペースを撲滅し、**全コンテナの充填率を限界（90%超）まで高め、結果的に使用コンテナ総数を物理的に削減する**」

現場の最大の悩みである**「納期絶対（送らなければならない）」**と**「コスト絶対（80%未満は赤字）」**という矛盾（トレードオフ）を、時間軸を前後に操作する最適化計算で解決することが最大の価値となります。

---

## 3. システム改修方針：追加・削除・変更リスト

### ❌ 【削除・除外するもの (Remove / Deprecate)】
1.  **「物流の波」を前提とした機能・UI**: 季節変動によるピーク/閑散期を均すような長期スパン（半年〜1年）の表示や平準化ロジックは実務と合わないため除外。
2.  **厳密すぎる積み込み順序（LIFO/FIFO）の制約**: 現場で「入れ替え可能」と明言されたため、奥から順番に積むような厳しい制約を外し、アルゴリズムの自由度（空間効率優先）を解放する。
3.  **時間軸を持たない単発の最適化**: その週の荷物「だけ」を見て終わる計算ロジック。

### ➕ 【追加・実装するもの (Add / Implement)】
1.  **「時間（期限）」データの付与**: 全ての荷物データに「梱包完了日」「納期（出荷リミット）」「待機限界（最大3週間）」のタイムスタンプ情報を持たせる。
2.  **時間軸操作ロジック（Pull & Push）**: 最後のコンテナが80%未満になった際、以下の2つのアプローチで最適化を図るアルゴリズム。
    *   **前倒し（Pull）**: 次週以降のリストから「前倒し可能」な荷物を引っ張ってきて80%以上にする。
    *   **繰り越し（Push / 待機）**: 納期に余裕がある場合、無理に出荷せず「保留プール」に入れ、次回の荷物と合流させて再計算する。
3.  **「保留プール（仮想待合室）」機能**: 今回出荷せずに次回へ繰り越す荷物を管理する機能。
4.  **ステータス判定とアラート表示（UI）**:
    各コンテナに対して以下の状態を視覚的に表示する。
    *   🟢 **正常クリア**: 充填率80%以上（納期・コストともにクリア）
    *   🟡 **前倒し補填 / 繰り越し合流**: 充填率80%以上（時間軸操作によって赤字を回避したコンテナ）
    *   🔴 **納期優先の赤字出荷**: 充填率80%未満（前倒しも繰り越しもできず、しかし納期限界のため赤字覚悟で強制出荷するコンテナ）

### 🔄 【変更・改良するもの (Modify / Refine)】
1.  **UIのタイムスケール**: 月単位のざっくりした切り替えから、実務の「月末に通達され、2〜4週間を計画する」という**ローリングホライズン（数週間単位でスライドしていく）**の画面構成に寄せる。
2.  **KPIダッシュボードの主役変更**: 単なる「削減本数」だけでなく、「80%超え達成率」や「時間軸操作によって救済した赤字コンテナの数」を表示し、システムの効果を直接的にアピールする。

---

## 4. その他・今後考えるべき考慮事項（Others / Risks）

時間軸操作（特に「繰り越し」）を行う上で、以下のリスクや制約をアルゴリズムに組み込む必要があります。

1.  **倉庫スペース（キャパシティ）の限界**:
    「効率化のために次の荷物を待つ（繰り越す）」場合、その荷物は日本の倉庫を占有し続けます。倉庫がパンクしないよう、保留プールの上限（体積・重量）をパラメータとして設定し、それを超える繰り越しは防ぐ必要があります。
2.  **「木箱の期限」と次期荷物の遅延リスク（チキンレース）**:
    荷物を繰り越して待っている間も、木箱のタイムリミット（3週間）は減り続けます。もし待っていた次期の荷物が生産遅れ等で届かなかった場合、木箱が腐る前に強制的に赤字出荷せざるを得ない最悪のケースが発生します。安全マージンを持った繰り越し判断が必要です。
3.  **パズルの組み合わせ爆発**:
    「前倒し」と「繰り越し」を同時に許可すると、システムが検討すべきパズルの組み合わせが膨大になります。人間が直感で決めるのは不可能なレベルになるため、ここにこそシステムの計算力が求められます。
4.  **材質による期限の違い**: 「木箱」は3週間限界ですが、スチール箱やパレットなどはもっと長く保留できるかなど、材質ごとの保管リミット設計。
5.  **現場の手動ロック機能**: 「この部品は製造遅れでまだ手元にないため前倒し不可」など、現場のイレギュラーに対応できるようUI上で特定荷物をロック（除外）する手動介入機能。

---
**結論：**
今回のヒアリングによってシステムの目的がより鋭利になりました。
**「人間では計算しきれない『倉庫キャパ』『木箱期限』『納期』『充填率』の多次元パズルを、未来のリストを活用した『前倒し・繰り越し（時間軸操作）』によって解き明かすソフト」**
これが、最終発表に向けた私たちの最強のコンセプトになります。